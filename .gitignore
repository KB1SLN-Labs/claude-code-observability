# claude code memory — personal session notes
.claude/

# local testing overrides — never commit these
docker-compose.override.yml
otel-collector/config.local.yaml
.env
.env.grafana-cloud
helm/values.local.yaml

# local editor config — personal, not project state
*.code-workspace

# personal Dynatrace working docs — not part of the public stack
DYNATRACE_PORT_SPEC.md
docs/DYNATRACE_*.md
